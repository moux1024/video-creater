# Context: video-creater

## Glossary

- **Project（项目）**：一次视频创作的完整工作空间，包含初始素材、各阶段产物、提示词配置与日志。
- **Stage（阶段）**：流水线中固定的一个步骤（意图理解、分镜、逐镜头提示词、首帧图、视频生成、拼接导出）。阶段骨架固定，不随输入类型变化。
- **Run（一次运行）**：某个阶段的一次执行及其全部产物。Run 是不可变的：重跑永远新建 Run，从不修改或删除旧 Run。
- **Parent Chain（溯源链）**：每个 Run 记录它执行时所消费的各上游阶段 Run 的引用。
- **Current Run（当前运行）**：某阶段被用户选中的 Run，作为下游阶段执行的输入来源。可随时切换到任意历史 Run。
- **Stale（过期）**：某 Run 的溯源链与其上游的 Current Run 不一致时的提示标记。Stale 仅是提示，不阻断查看、使用或导出。
- **Skill（技能）**：以 Markdown 文件形式存在的可复用指令包，用户可自行编写，按项目激活。
- **System Prompt Layers（系统提示词层）**：全局 → 项目 → 激活的 Skill → 阶段覆盖，逐层追加，后者优先。
- **Role（模型角色）**：模型配置的类别：Agent 大脑、文本、图像、视频生成，各角色独立配置 endpoint 与 key。
